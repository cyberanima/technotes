# UART
 There are two UARTs in ESP32S3 -- UART0 and ```UART1```.  
 UART0 is used for debugging and communication with the host computer. (GPIO1 and GPIO3)  
 UART1 is used for communication with other devices. (```GPIO43``` and ```GPIO44```)
![alt text](L07/image-1.png ':size=900')  
![alt text](L07/image-4.png ':size=500')![alt text](L07/image-3.png ':size=400')  
![alt text](L07/image-5.png ':size=450')![alt text](L07/image-6.png ':size=450')

## 1. LED Toggle Remote Control
Conect Two ESP32S3s -- Master and Slave. Master sends the command to Slave to toggle the LED. The conmmand is 't'.
```python
# Master
from machine import UART
from time import sleep



```

## 2. Chat
in shell input()

## 3. Remote DVM
```e.g.``` Master sends data and Slave displays the data on OLED.

```Assessment```Make a remote DVM. Master measure the voltage and sends the data to Slave. Slave receives and displays the data on OLED. 